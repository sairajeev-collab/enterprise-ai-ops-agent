# Auto-provision the Prometheus datasource so the bundled dashboard resolves its
# `prometheus` uid without any manual clicking.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
