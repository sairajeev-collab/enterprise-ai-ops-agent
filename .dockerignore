.git
.gitignore
.venv
venv
__pycache__
*.py[cod]
.pytest_cache
.mypy_cache
.ruff_cache
.coverage
coverage.xml
htmlcov
.env
.env.*
!.env.example
docs
tests
# Ignore markdown, but keep README.md: the Dockerfile COPYs it and pyproject
# declares it as the package readme, so the build needs it in context.
*.md
!README.md
